import React from "react";
import ReactDOM from "react-dom/client";
import GeoMemoApp from "./GeoMemoApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GeoMemoApp />
  </React.StrictMode>
);
